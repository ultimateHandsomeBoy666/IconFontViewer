# IconFontViewer Plugin 改造 PRD

## 1. 项目概述

### 1.1 项目背景
IconFontViewer 是一个 Android Studio / IntelliJ IDEA 插件，用于在编辑器中预览图标字体（IconFont）。当前版本在 TTF 文件管理、用户体验和功能完整性方面存在改进空间，需要进行全面升级改造。

### 1.2 项目目标
- 提升 TTF 文件扫描和管理能力
- 优化用户界面和交互体验
- 增强插件的智能化程度
- 提高插件的稳定性和性能

### 1.3 目标用户
- Android 开发工程师
- 使用图标字体的移动应用开发者
- 需要在 IDE 中预览图标的设计师和开发者

## 2. 功能需求

### 2.1 TTF 文件扫描与管理系统

#### 2.1.1 智能扫描功能

**功能描述**：在项目启动时自动扫描项目中所有可用的 TTF 文件

**详细需求**：
- **项目文件扫描**：扫描项目 `src/main/assets/fonts/`、`src/main/res/font/` 等目录下的 TTF 文件
- **AAR 依赖扫描**：扫描项目所有 Gradle 依赖的 AAR 包中包含的 TTF 文件
- **智能过滤**：
  - 排除 `build/`、`.git/`、`.idea/` 等配置目录
  - 排除临时文件和缓存文件
  - 支持自定义排除规则
- **源信息记录**：记录每个 TTF 文件的来源信息（项目文件 / AAR 依赖包名及版本）

**优先级**：P0

#### 2.1.2 启发式智能分类与自动启用

**功能描述**：基于文件名特征，自动判断 TTF 文件是否为图标字体并自动启用

**启发式规则**：
- **图标字体关键词匹配**：文件名包含 `icon`、`iconfont`、`fontawesome`、`material`、`symbol` 等关键词
- **自动启用策略**：命中启发式规则的 TTF 文件直接自动启用，无需用户手动干预
- **普通字体处理**：未命中规则的 TTF 文件默认禁用，用户可通过开关手动启用

**处理逻辑**：
- 扫描到的 TTF → 启发式判断 → 自动启用/默认禁用
- 手动添加的 TTF → 强制启用（无开关控制）

**UI展示规则**：
- **不显示提示信息**：不在UI中显示"建议启用"等提示文字
- **通过状态区分**：启用/禁用状态通过视觉效果（高亮/置灰）直接体现
- **简洁界面**：避免冗余的文字提示，保持界面简洁

**优先级**：P0

#### 2.1.3 用户手动添加功能

**功能描述**：支持用户手动添加系统中的 TTF 文件

**详细需求**：
- 提供文件选择器，支持 `.ttf` 格式过滤
- 手动添加的 TTF 强制启用，无需用户二次确认
- 支持删除手动添加的 TTF 文件
- 手动添加的文件在插件重启后保持记忆

**优先级**：P1

### 2.2 TTF 管理界面系统

#### 2.2.1 现代化 UI 设计

**设计稿参考**：[ttf_ui_design.html](./ttf_ui_design.html)

**设计特点**：
- 采用 IntelliJ IDEA 2023+ Darcula 主题风格
- 使用 JetBrains Mono 字体系统
- 现代化圆角、渐变和阴影效果
- 响应式布局和流畅动画

**优先级**：P1

#### 2.2.2 TTF 列表展示

**功能描述**：以列表形式展示所有扫描到和手动添加的 TTF 文件

**列表项信息**：
- **TTF 图标预览区域**：
  - **图标字体**：显示实际图标字符（⭐、🏠、❤️），蓝色渐变背景 + 阴影
  - **普通字体**：显示通用 "T" 字符，深灰色背景
  - **交互**：扫描类型的TTF点击图标区域可切换开关
- **文件信息区域**：
  - **文件名**：主标题，启用时高亮显示，禁用时置灰
  - **文件路径**：副标题，等宽字体显示完整路径
  - **AAR 信息**：显示具体的AAR包名和版本（如适用）
- **控制区域**：
  - **来源标签**：项目扫描(绿)、AAR依赖(蓝)、手动添加(橙)
  - **扫描类型TTF**：显示开关，控制启用/禁用状态
  - **手动添加TTF**：显示删除按钮，无开关（强制启用状态）

**优先级**：P0

#### 2.2.3 视觉状态反馈

**功能描述**：通过视觉效果清晰展示 TTF 的启用状态

**状态设计**：
- **启用状态**：正常亮度显示，图标和文字清晰可见
- **禁用状态**：整体半透明 (opacity: 0.5)，禁用 hover 效果
- **交互反馈**：
  - hover 时背景色微调
  - 开关切换有流畅动画
  - 按钮有 hover 和 active 状态

**优先级**：P0

#### 2.2.4 操作功能

**底部操作栏**：
- **添加 TTF 文件**：主要按钮，蓝色渐变样式，打开文件选择器
- **重新扫描**：次要按钮，重新执行项目和AAR扫描

**交互逻辑**：
- **扫描类型TTF**：
  - 通过开关控制启用/禁用状态
  - 点击图标区域也可切换开关
  - 状态变化时整个列表项有视觉反馈（高亮/置灰）
- **手动添加TTF**：
  - 强制启用状态，永远高亮显示
  - 只显示删除按钮，无开关控制
  - 删除后从插件中完全移除

**优先级**：P0

### 2.3 数据模型升级

#### 2.3.1 TTF 文件模型扩展

```kotlin
data class IconFontTtfFileModel(
    var enabled: Boolean,              // 是否启用
    var ttfFileName: String,           // 文件名
    var ttfAbsolutePath: String,       // 绝对路径
    var font: Font,                    // 字体对象
    var source: TTFSource,             // 来源信息
    var type: TTFType,                 // 分类类型
    var userModified: Boolean = false   // 用户是否手动修改过状态
)

enum class TTFType {
    ICON_FONT,      // 图标字体（命中启发式规则）
    REGULAR_FONT    // 普通字体（未命中规则）
}

sealed class TTFSource {
    object ProjectAssets : TTFSource()
    object UserAdded : TTFSource()
    data class AARDependency(val aarName: String, val version: String) : TTFSource()
}
```

**优先级**：P0

#### 2.3.2 数据持久化

**功能需求**：
- 保存用户的启用/禁用选择
- 保存手动添加的 TTF 文件路径
- 支持跨项目的全局配置

**优先级**：P1

## 3. 技术实现要点

### 3.1 扫描实现
- 使用 `FilenameIndex.getAllFilenames()` 扫描项目文件
- 通过 Android Gradle Plugin API 获取 AAR 依赖信息
- 实现增量扫描，避免重复处理

### 3.2 UI 实现
- 基于 IntelliJ Platform UI 组件
- 使用 `JBCheckBox`、`JBButton` 等标准组件
- 实现自定义 TTF 图标渲染器

### 3.3 性能优化
- 后台线程执行扫描任务
- 懒加载字体对象，避免内存占用
- 缓存扫描结果，提高响应速度

## 4. 用户故事

### 4.1 自动扫描场景
**作为**开发者
**我想要**插件自动发现项目中的图标字体
**以便**我无需手动配置就能在编辑器中看到图标预览

**验收标准**：
- 项目启动后自动扫描完成
- 文件名包含 "iconfont" 的 TTF 自动启用
- 在代码编辑器中能看到对应的图标预览

### 4.2 手动管理场景
**作为**开发者
**我想要**能够手动添加和管理 TTF 文件
**以便**我可以使用项目外部的图标字体

**验收标准**：
- 可以通过文件选择器添加 TTF
- 手动添加的 TTF 强制启用且无开关控制
- 可以删除手动添加的 TTF（扫描的TTF不能删除）

### 4.3 状态管理场景
**作为**开发者
**我想要**清楚地知道哪些 TTF 正在被使用
**以便**我可以精确控制图标预览的范围

**验收标准**：
- 启用的 TTF 显示高亮状态
- 禁用的 TTF 显示置灰状态
- 可以通过开关随时切换状态

## 5. 非功能性需求

### 5.1 性能要求
- TTF 扫描时间不超过 5 秒（中等规模项目）
- UI 响应时间不超过 100ms
- 内存占用控制在合理范围内

### 5.2 兼容性要求
- 支持 Android Studio Dolphin 及以上版本
- 支持 IntelliJ IDEA 2021.3 及以上版本
- 兼容 Windows、macOS、Linux 系统

### 5.3 稳定性要求
- 插件加载失败率 < 1%
- 扫描过程不影响 IDE 正常使用
- 异常情况下有合适的错误提示

## 6. 项目里程碑

### 阶段一：核心功能实现 (Week 1-2)
- [ ] TTF 扫描系统实现
- [ ] 数据模型升级
- [ ] 基础 UI 框架搭建

### 阶段二：UI 和交互优化 (Week 3-4)
- [ ] 现代化 UI 设计实现
- [ ] 视觉状态反馈完善
- [ ] 用户操作功能实现

### 阶段三：测试和优化 (Week 5)
- [ ] 功能测试和 Bug 修复
- [ ] 性能优化
- [ ] 用户体验调优

### 阶段四：发布准备 (Week 6)
- [ ] 文档更新
- [ ] 版本发布准备
- [ ] 用户反馈收集

## 7. 风险与依赖

### 7.1 技术风险
- AAR 依赖扫描的复杂性可能超出预期
- 不同版本 IDE 的兼容性问题
- 字体加载性能影响

### 7.2 外部依赖
- IntelliJ Platform API 稳定性
- Android Gradle Plugin API 变更
- 系统字体 API 的差异性

---

**文档版本**：v1.0
**创建时间**：2025-01-13
**最后更新**：2025-01-13
**负责人**：开发团队