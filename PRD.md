# IconFontViewer 插件重构与功能升级 PRD

**版本**: 1.0

**状态**: 草稿

**最后更新**: 2025-10-11

---

## 1. 项目概述

### 1.1. 背景

`IconFontViewer` 插件为开发者提供了在 IDE 中预览 IconFont 的能力，但现有版本在功能、性能和用户体验上已逐渐无法满足现代化的开发需求。例如，字体文件的发现机制单一、缺少有效的管理界面、UI 风格与新版 IDEA 不兼容等。

为了提升插件的核心价值和用户满意度，有必要进行一次全面的重构与功能升级。

### 1.2. 核心目标

- **提升易用性**: 实现“零配置”的开箱即用体验，同时提供灵活、直观的管理选项。
- **增强功能性**: 拓展插件的能力边界，支持更多字体来源和使用场景。
- **现代化 UI/UX**: 更新插件的整体视觉风格，使其与最新的 IntelliJ Platform UI 保持一致。
- **优化性能与稳定性**: 确保插件在处理大量资源时依然保持流畅，并减少潜在的内存泄漏和错误。

---

## 2. 核心功能模块

### 2.1. **模块一：TTF 字体管理与扫描机制 (已规划)**

此模块旨在彻底优化字体的获取、管理和预览机制。

#### 2.1.1. 功能点详情

- **多源字体获取**:
  - **自动扫描**: 在 IDE 启动后，后台自动扫描**项目源文件**及所有 **AAR 依赖**中的 `.ttf` 文件。
  - **手动添加**: 用户可通过管理界面的“添加”按钮，从本地文件系统中选择任意 `.ttf` 文件进行添加。

- **全新的字体管理面板**:
  - **统一列表**: 集中展示所有来源（项目、AAR、手动添加）的字体，并清晰标示其来源、路径等信息。
  - **启发式判断**: 扫描到的字体将根据文件名（如 `icon`, `iconfont`）自动判断是否为 IconFont，并相应地**默认开启/关闭**预览功能。
  - **可控启用**: 每一个字体，不论是手动添加还是扫描到的，都提供「开关」switch 和「删除」按钮。开关 switch 可以控制当前是否激活或关闭这个font，删除则可以直接把这个字体删掉。
  - **状态反馈**: 激活的字体条目**高亮**显示，未激活的则**置灰**显示。
  - **全局操作**: 提供“刷新列表”和“添加字体文件”的全局操作按钮。

- **预览逻辑联动**: Gutter 图标的渲染将严格依据管理面板中字体的“激活”状态进行。

#### 2.1.2. 设计资源

- **[点击查看可交互的 UI 设计稿 (新版 IDEA 风格)](./ttf_ui_design.html)**

---

### 2.2. **模块二：UI/UX 全面革新 (待规划)**

- **目标**: 将插件的所有窗口、弹窗和通知，全面迁移至新版 IntelliJ Platform 的 UI 风格。
- **范围**: (此部分待后续讨论填充，例如：Gutter 点击后的图标选择弹窗、主管理窗口的整体框架等)

---

### 2.3. **模块三：性能与稳定性优化 (待规划)**

- **目标**: 优化核心流程的性能，修复潜在的稳定性问题。
- **范围**: 
  - **后台处理**: 确保所有文件 I/O 和计算密集型任务（如字体解析、文件扫描）都在后台线程执行，避免 UI 卡顿。
  - **缓存策略**: 为已解析的字体和图标建立高效的缓存与索引机制（如从 `List` 查询优化为 `Map` 查询）。
  - **内存管理**: 审查并使用 `SmartPsiElementPointer` 等机制，防止 `PsiElement` 持有导致的内存泄漏。
  - **错误处理**: 建立完善的错误处理机制，对字体文件损坏或无法访问的情况进行妥善处理。

---

### 2.4. **模块四：其他功能增强 (待规划)**

- **目标**: 根据用户反馈和需求，增加更多提升开发效率的功能。
- **想法池**: (此部分待后续讨论填充，例如：支持 SVG 图标、支持从网络 URL 添加字体等)

